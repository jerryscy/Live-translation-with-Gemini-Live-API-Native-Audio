# ---------------------------------------------------------------------------
# Main translation app  (Python >= 3.10; tested on Python 3.14)
# Install into .venv-app:
#   /opt/homebrew/bin/python3.14 -m venv .venv-app
#   ./.venv-app/bin/pip install --index-url https://pypi.org/simple -r requirements.txt
# ---------------------------------------------------------------------------
google-genai==2.10.0          # latest SDK; needs Python >= 3.10
fastapi
uvicorn[standard]
python-dotenv
websockets                    # WebSocket server (browser <-> backend)
authlib                       # Google OAuth (OpenID Connect) login flow
itsdangerous                  # signs the session cookie (SessionMiddleware)
httpx                         # async HTTP client used by authlib for OIDC

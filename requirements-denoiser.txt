# ---------------------------------------------------------------------------
# DeepFilterNet2 denoiser sidecar  (Python 3.8-3.11 ONLY; tested on 3.9)
# The native DeepFilterLib wheel exists only for CPython 3.8-3.11 and requires
# numpy<2, which is why the denoiser runs in its own interpreter.
# Install into .venv:
#   /usr/bin/python3 -m venv .venv           # system Python 3.9
#   ./.venv/bin/pip install --index-url https://pypi.org/simple -r requirements-denoiser.txt
# ---------------------------------------------------------------------------
numpy==1.26.4
torch==2.8.0
torchaudio==2.8.0
deepfilternet==0.5.6
websockets
python-dotenv
